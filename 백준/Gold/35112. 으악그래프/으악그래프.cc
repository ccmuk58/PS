#include<bits/stdc++.h>
using namespace std;

int main()
{
    int N, M;
    cin >> N >> M;
    
    for(int i=0; i<M; i++)
    {
        int A, B;
        cin >> A >> B;
    }
    
    if(M<=N)cout << "Yes";
    else cout << "No";
}